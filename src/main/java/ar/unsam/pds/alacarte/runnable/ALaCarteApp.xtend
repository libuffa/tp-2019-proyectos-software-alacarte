package ar.unsam.pds.alacarte.runnable

class ALaCarteApp {
	
	def static void main(String[] args) {
		new ALaCarteBootstrap().run
//		XTRest.start(9000, UsuarioController, EventosController, InvitacionController, LocacionController, EntradaController)
	}
	
}